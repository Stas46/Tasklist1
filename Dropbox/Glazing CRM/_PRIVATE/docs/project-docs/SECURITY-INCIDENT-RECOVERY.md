# 🔐 Инструкция по восстановлению после утечки Telegram Bot Token

## ✅ Что уже сделано

1. **Токен удален из публичных файлов**
   - `docs/n8n-integration-plan.md` - заменен на переменную окружения
   - `n8n/workflows/telegram-notifications.json` - используется `{{$credentials.telegramBotToken}}`
   - `n8n/workflows/ai-actions.json` - используется `{{$credentials.telegramBotToken}}`

2. **История Git полностью очищена**
   - Использован `git-filter-repo` для удаления токена из всех коммитов
   - Сделан force push в GitHub
   - Токен больше не найден в истории: `git grep "8508895640" $(git rev-list --all)` = 0 результатов

## 🚨 ЧТО НУЖНО СДЕЛАТЬ СРОЧНО

### 1. Отозвать старый токен в BotFather

```
1. Откройте Telegram
2. Найдите @BotFather
3. Отправьте: /mybots
4. Выберите вашего бота
5. Нажмите "API Token"
6. Нажмите "Revoke current token"
7. Подтвердите отзыв
8. Скопируйте НОВЫЙ токен
```

### 2. Обновить токен в переменных окружения

#### Локально (`.env.local`):
```bash
TELEGRAM_BOT_TOKEN=ваш_новый_токен
```

#### На сервере:
```bash
ssh root@82.97.253.12
nano /var/www/alu.stella-spb.ru/.env.local
# Замените TELEGRAM_BOT_TOKEN на новый
# Ctrl+X, Y, Enter

# Перезапустите приложение
pm2 restart crm
```

### 3. Обновить токен в n8n

1. Откройте https://n8n.alu.stella-spb.ru
2. Логин: admin / your_password
3. Settings → Credentials → Add Credential
4. Выберите "Telegram"
5. Введите новый Bot Token
6. Сохраните как "Telegram Bot"

### 4. Обновить workflows в n8n

Импортируйте обновленные workflows:
- `n8n/workflows/telegram-notifications.json`
- `n8n/workflows/ai-actions.json`

Они теперь используют credentials вместо hardcoded токена.

### 5. Закрыть GitGuardian alert

1. Перейдите по ссылке из письма GitGuardian
2. Отметьте инцидент как "Revoked"
3. Добавьте комментарий: "Token revoked via @BotFather, git history cleaned with git-filter-repo, new token configured"

## 📋 Проверка

После выполнения всех шагов:

```bash
# 1. Проверьте, что токен удален из истории
cd /path/to/project
git grep "8508895640" $(git rev-list --all)
# Должно быть 0 результатов

# 2. Проверьте, что новый токен работает
curl "https://api.telegram.org/bot<НОВЫЙ_ТОКЕН>/getMe"
# Должно вернуть информацию о боте

# 3. Проверьте, что старый токен НЕ работает
curl "https://api.telegram.org/bot8508895640:AAHFMGty1dQRwZ03uU0zRiRnLKkQaOlrSI8/getMe"
# Должно вернуть ошибку "Unauthorized"
```

## 🛡️ Меры безопасности на будущее

1. **НИКОГДА** не коммитьте токены в Git
2. Всегда используйте переменные окружения
3. Добавьте `.env*` в `.gitignore`
4. Используйте secrets management (например, 1Password, Vault)
5. Регулярно ротируйте токены
6. Включите GitHub Secret Scanning Alerts

## 📁 Файлы, которые были изменены

- `docs/n8n-integration-plan.md` - удален hardcoded токен
- `n8n/workflows/telegram-notifications.json` - использует credentials
- `n8n/workflows/ai-actions.json` - использует credentials
- `.gitignore` - убедитесь, что `.env*` в списке
